import React from 'react'
import LoginPage from '../../components/LoginPage'

export default function Login({loading,setloading}) {
  return (
  <LoginPage  loading={loading} setloading={setloading} />
  )
}
